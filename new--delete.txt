c++中的delete
1.不要对同一个指针多次delete
2.delete成功后.将指针置为NULL;
3.delete NULL指针无害,无需加判断语句;
4.对于有指针成员的类
       如果用new new一个数组,需要用delete[]来释放,不然会造成内存的泄露,但是泄露的内存并不是指new数组所开辟的内存空间,而是指针所指向的那一片内存
原因是它的析构函数没有被delete调用;
